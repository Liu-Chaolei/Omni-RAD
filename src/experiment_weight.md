实验名称：解码两条支路的权重占比
背景：λ 在注入 U-Net 的时候会生成一个 scale 参数 (范围[0,2]) 对 U-Net 的输出进行放缩
目的：
要证明低码率下模型更依赖 U-Net 生成支路
λ 在这个过程中起到了有效调控作用
实验：记录不同 λ 下，scale 参数的值、AUX 支路和U-Net 支路输出的 L2 范数
预期：scale、 AUX/U-Net 支路输出的比值都随着 λ 单调变化