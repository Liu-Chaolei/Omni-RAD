# sc 相对 StableCodec 的创新点与方法贡献

> 本文按各项机制的有效性已获实验验证这一前提展开，重点讨论方法设计、解决的问题及相对 StableCodec 的贡献。

## 1. 总体定位：码率与压缩状态共同驱动的自适应单步扩散压缩

`sc` 将 StableCodec 的固定工作点压缩与固定解码策略，扩展为由码率条件、压缩状态和空间特征共同控制的自适应压缩方法。

其方法主线可以概括为：**以连续码率条件协调编码与重建，以压缩统计选择扩散时间步，以熵模型不确定性调节生成与结构恢复的融合。**

这条主线包含三个互补层次：

| 控制层次 | 回答的问题 | 方法机制 |
| --- | --- | --- |
| 码率工作点 | 在什么压缩强度下编码和重建？ | λ 条件化的多位置 FiLM 与噪声预测幅度调制 |
| 图像级解码 | 当前压缩表示适合使用哪个去噪时间步？ | 压缩 SNR 引导的时间步选择与 PolicyNet 修正 |
| 空间及通道级融合 | 各位置、各特征需要多少生成修复与结构补偿？ | 熵尺度引导的门控、双门控与条件特征变换 |

这些机制构成统一的方法逻辑。A1/A2/A3 和 CFT/CFTv2 是融合机制的不同实现形式，在贡献表述中应作为同一方向下的结构设计展开。

## 2. 创新点一：面向单步扩散压缩的连续码率协同调制

### 2.1 解决的问题

原版 StableCodec 通过不同码率权重分别微调模型，依赖多个 checkpoint 覆盖不同工作点。对于生成式压缩，码率变化还会改变传输表示的完整程度，以及重建时需要依赖生成先验的程度。

因此，连续可变码率需要同时处理两个问题：让编码器根据压缩要求组织信息，让解码器根据相同要求调整信息恢复方式。

### 2.2 方法设计

首先，将 λ 转换为共享条件表示：

```text
λ → log 归一化 → Fourier 编码 → MLP → f(λ)
```

随后，在分析变换、合成变换和辅助解码器的多个位置进行条件化仿射调制：

```text
h_i′ = γ_i(f(λ)) ⊙ h_i + β_i(f(λ))
```

共享的 `f(λ)` 提供一致的工作点信息，各位置独立学习自己的缩放与偏移，从而让不同模块对同一压缩要求作出不同响应：

- **分析变换 `g_a`**：调整编码特征的提取与组织，影响需要传输的信息。
- **合成变换 `g_s`**：调整送入扩散网络的潜表示，使其适应当前压缩工作点。
- **辅助解码器**：调整辅助结构信息的恢复方式。
- **扩散预测输出**：通过 `s(λ) = 1 + tanh(P(f(λ)))` 调节噪声预测幅度。

最后一项在实现中对应 `model_pred = model_pred * lora_scale`，准确的方法名称是“码率条件化的噪声预测幅度调制”。

训练时逐图采样 λ，并使用以下基础目标覆盖连续工作区间：

```text
L = mean_i[D_i / λ_i + R_i]
```

其中，λ 越大，码率项相对越重要，模型倾向于采用更低的 bpp。λ 是率失真权衡参数。

### 2.3 创新与贡献

该设计的贡献在于**将连续码率控制贯穿压缩表示形成、扩散输入构造、结构补偿和生成预测的多个环节**，建立编码与生成式解码之间的协同适配。

它使单个模型具有连续调节率失真工作点的能力，同时让生成过程感知压缩要求，降低多模型训练、存储和部署成本。

从方法表述上，核心是“面向扩散压缩的多位置联合条件化”。Fourier 编码和 FiLM 是实现这一设计的技术组件。

**代码对应：** [latent_codec_variable2_step.py](src/latent_codec_variable2_step.py) 中的 `LambdaFiLMEmbed`、`FiLMLayer`、各条件化变换，以及 [StableCodec_variable2_step.py](src/StableCodec_variable2_step.py) 中的预测幅度调制。

## 3. 创新点二：压缩状态感知的自适应单步扩散时间步

### 3.1 解决的问题

原版固定使用 `t = 999`。不同图像和不同码率下，压缩潜变量的统计特征、保留信息和重建需求不同，固定时间步无法显式响应这些差异。

时间步决定 UNet 接收的噪声级别条件，也参与从预测噪声恢复潜变量的计算。因此，选择时间步是调节单步重建行为的重要途径。

### 3.2 方法设计

第一层控制利用熵模型尺度构造压缩 SNR 代理量：

```text
熵模型尺度统计 → 压缩 SNR 估计 → 扩散 SNR 调度映射 → T_snr
```

基础实现使用尺度均值的平方作为信号方差代理，结合量化噪声方差近似，获得图像级压缩状态描述，再映射到限定时间步范围。

第二层控制使用轻量 PolicyNet 学习内容相关的修正。输入包括 λ、码率、熵尺度统计、潜变量能量、辅助残差统计等，输出有界时间步偏移：

```text
ΔT = ΔT_max · tanh(PolicyNet(features))
T = clip(T_snr + ΔT, T_min, T_max)
```

这种结构将统计规则提供的基准与可学习的内容适配结合起来。基准确定时间步的大致工作区域，PolicyNet 利用更丰富的特征修正逐图差异。

策略版本还对扩散调度中的累积系数进行连续插值，使重建损失能够通过调度计算反馈到时间步预测网络；相应训练提供策略单独学习与部分模块联合适配的阶段安排。

### 3.3 创新与贡献

该设计将**压缩表示的统计状态与扩散模型的时间条件建立显式联系**，使解码策略随图像内容和压缩工作点变化。

其贡献主要体现在：

- **压缩与生成之间的控制联系**：熵模型统计参与生成式重建策略选择。
- **规则与学习结合的时间步预测**：以压缩 SNR 为基准，通过有界残差学习逐图修正。
- **保留单步解码形式**：自适应选择一次去噪所使用的时间步位置，维持单步扩散的计算结构。

这里的 SNR 是用于控制的统计代理量，方法名称可使用“压缩状态感知的时间步选择”或“熵统计引导的自适应扩散时间条件”。

**代码对应：** [latent_codec_variable2_step.py](src/latent_codec_variable2_step.py) 中的 `DynamicTimestepModule`、[timestep_policy_net.py](src/timestep_policy_net.py) 中的特征构建与 `TimestepPolicyNet`，以及 [StableCodec_variable2_step_policy.py](src/StableCodec_variable2_step_policy.py) 中的连续调度计算与分阶段训练设置。

## 4. 创新点三：熵模型不确定性引导的生成—结构自适应融合

### 4.1 解决的问题

原版将扩散去噪结果与辅助解码残差直接相加：

```text
z_hat = z_diff + r_aux
```

这种融合使用固定形式，没有显式区分不同空间位置和特征通道对生成修复、结构补偿的需求。自然图像的平滑区域、边缘和纹理区域具有不同的统计特征，压缩后可恢复的信息也不同。

`sc` 利用熵模型预测的条件尺度作为不确定性特征，学习控制两类重建信息的作用位置和贡献程度。

### 4.2 空间门控与通道—空间门控

A1 对尺度图进行通道聚合、空间归一化和分辨率对齐，再生成空间门控：

```text
M = sigmoid(a · Normalize(ScaleMap) + b)
z_hat = z_diff + M ⊙ r_aux
```

A2 将尺度特征映射为通道—空间门控，使不同通道在不同位置拥有独立的调节能力。

这两种结构将辅助残差的注入由固定相加扩展为不确定性条件化的细粒度控制。

### 4.3 生成修复与结构补偿的双门控

A3 将扩散输出分解为输入潜变量与生成修复增量：

```text
Δz_diff = z_diff − z_input
z_hat = z_input + M_diff ⊙ Δz_diff + M_aux ⊙ r_aux
```

两个门控分别控制生成修复和辅助结构补偿，使模型可以针对同一区域独立调节两条路径。

这一分解给出了清晰的功能解释：模型同时决定“对压缩潜表示做多少生成修复”和“从辅助路径补充多少结构信息”。

### 4.4 条件特征变换与图像级融合强度预测

CFT 将重建特征、辅助特征、熵尺度特征和 λ 条件结合，预测仿射变换参数。其核心形式为：

```text
(α, β) = F(z_diff, r_aux, scale_features, f(λ))
z_cft = z_diff + w · (α ⊙ z_diff + β)
```

辅助路径通过条件特征参与变换参数预测，提供比逐元素乘性门控更丰富的融合能力。

CFTv2 进一步引入 `WPolicyNet`，根据全局统计预测图像级融合强度 `w`，与空间及通道级的仿射参数共同决定融合行为。

训练通过渐进系数 ρ 从原有直接融合过渡到自适应融合：

```text
z_hat = (1 − ρ) · z_direct + ρ · z_adaptive
```

### 4.5 创新与贡献

这一方向的核心贡献是**将熵模型中的条件不确定性信息用于生成与结构恢复的分配**，使概率建模与重建控制共享压缩统计信息。

空间门控、双门控和 CFT 分别提供不同的控制能力：

| 结构 | 控制对象 | 方法贡献 |
| --- | --- | --- |
| A1 | 辅助残差的空间注入量 | 建立熵尺度与局部结构补偿的联系 |
| A2 | 辅助残差的通道及空间注入量 | 提供更细粒度的特征选择 |
| A3 | 生成修复与辅助残差两类增量 | 独立调节生成修复和结构补偿 |
| CFT | 重建潜变量的条件仿射变换 | 增强融合的表达能力 |
| CFTv2 | 条件仿射变换及图像级强度 | 结合图像级决策与局部特征调节 |

从论文组织上，这些结构适合归纳为“熵不确定性引导的自适应融合”，突出压缩统计如何驱动生成式重建。

**代码对应：** [A1](src/StableCodec_variable2_step_A1.py)、[A2](src/StableCodec_variable2_step_A2.py)、[A3](src/StableCodec_variable2_step_A3.py)、[CFT](src/StableCodec_variable2_step_CFT.py)、[CFTv2](src/StableCodec_variable2_step_CFTv2.py)。

## 5. 支撑性贡献：潜表示扩展与训练适配

### 5.1 扩展潜表示容量

相关分支将原版 4 通道 VAE 潜空间扩展为 256 通道，并相应调整分析变换、辅助解码器及扩散相关接口。

这项设计提供更宽的中间表示，为结构信息和生成相关特征提供表达空间。传输码率仍由后续熵编码约束，因此可以从“中间表示容量与实际传输预算分别设计”的角度解释其作用。

这项贡献适合表述为“面向压缩重建的潜表示容量适配”，作为连续码率和自适应解码的结构基础。

**代码对应：** [latent_codec.py](src/latent_codec.py) 中的 `AnalysisTransform`、`AuxDecoder`，以及相关 StableCodec 模型接口。

### 5.2 面向可变码率和自适应控制的训练设计

配套训练机制包括：

- **逐图 λ 采样与率失真归一化**：使一个 batch 中的不同图像可以处于不同压缩工作点。
- **非均匀 λ 采样与码率区间约束**：调整训练对目标工作区间的关注程度。
- **FiLM 恒等初始化与融合渐进调度**：为引入新控制模块提供平滑的适配过程。
- **策略单独学习与联合适配**：先学习时间步策略，再让部分生成及条件化模块配合策略调整。

这些训练设计服务于主方法的学习与适配，适合与对应模块共同介绍。

**代码对应：** [trainv1_variable3_debug_step6.py](src/trainv1_variable3_debug_step6.py)、[StableCodec_variable2_step_policy.py](src/StableCodec_variable2_step_policy.py)，以及各 FiLM、门控和 CFT 模块。

## 6. 相对 StableCodec 的方法贡献对照

| 维度 | StableCodec | sc 的扩展 | 核心贡献 |
| --- | --- | --- | --- |
| 码率控制 | 不同码率权重对应分别微调的模型 | 单模型接收连续 λ 条件 | 连续工作点覆盖与编码—重建协同适配 |
| 扩散时间条件 | 固定时间步 | 压缩统计驱动的时间步及学习修正 | 图像级自适应单步重建 |
| 双分支融合 | 去噪结果与辅助残差直接相加 | 不确定性门控、双门控及 CFT | 生成修复与结构补偿的细粒度调节 |
| 熵模型的作用 | 概率估计与熵编码 | 同时提供重建控制特征 | 连接概率建模与生成式解码 |
| 潜表示设计 | 原版 4 通道 VAE 潜空间 | 相关分支扩展到 256 通道 | 面向压缩重建的表示容量适配 |
| 学习方式 | 面向指定工作点的优化 | 可变 λ 训练与控制模块渐进适配 | 支持多个工作点和自适应解码行为 |

## 7. 可直接用于论文的贡献表述

本文在单步扩散图像压缩框架中引入码率与压缩状态驱动的自适应控制，主要贡献如下：

1. **提出连续码率条件化的单步扩散压缩方法。** 通过共享码率嵌入协调分析变换、合成变换、辅助结构解码与噪声预测幅度，使单个模型能够覆盖连续率失真工作点，并实现压缩表示与生成式重建的协同适配。

2. **提出压缩状态感知的自适应扩散时间步选择机制。** 利用熵模型尺度构造压缩 SNR 基准，并通过轻量策略网络学习内容相关的有界时间步修正，使单步解码根据图像内容和压缩状态调整重建行为。

3. **提出熵模型不确定性引导的自适应融合机制。** 将熵尺度用于空间及通道门控和条件特征变换，调节生成修复与辅助结构信息的贡献，建立概率建模与生成式重建之间的联系。

整项工作的核心思想是：**让熵模型提供的压缩统计同时参与比特编码、图像级解码决策和局部重建控制，并通过连续码率条件协调这些过程。**
